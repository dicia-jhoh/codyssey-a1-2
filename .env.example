# 이 파일을 복사해 .env 로 이름을 바꾸고 실제 키를 채우세요.
#   cp .env.example .env
# .env 는 .gitignore 에 있어 GitHub 에 올라가지 않습니다.

# LLM (1단계 — 여행지 추천 JSON 생성)
OPENAI_API_KEY=YOUR_KEY

# 지도/장소 검색 (2단계 — 맛집 검색)
KAKAO_REST_API_KEY=YOUR_KEY
